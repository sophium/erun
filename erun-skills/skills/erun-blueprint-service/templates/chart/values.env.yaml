# __ENV__ overlay for __COMPONENT__. Copy this file to values.<env>.yaml for
# every environment this component deploys to — erun deploy requires one per
# chart per env with no fallback. A comment-only file (like this one, as
# shipped) is valid; add real overrides (replicas, resources, extraEnv) as
# this environment needs them.
replicas: 1

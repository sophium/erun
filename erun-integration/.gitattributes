# Goldens are compared byte-for-byte against output the erun binary writes with
# LF line endings. A Windows checkout with core.autocrlf=true would otherwise
# materialize them as CRLF and fail every comparison on text that reads
# identically, so the working-tree ending is pinned here rather than left to the
# developer's git config.
testdata/**/*.txt text eol=lf

audit: erun deploy --version <VERSION> team dev
trace-log: appending the full trace to <TMP>
deploy: tenant=team environment=dev version-override=<VERSION> components=[] force=false current=false
deploy: configured repo path /nonexistent-remote/team is not present on this machine; no k8s.deployments plan could be read
deploy: component selection source default (runtime only); deploying the runtime chart alone
deploy: runtime chart team-devops <VERSION> not found in registry.example/test (the tenant's own umbrella)
deploy: runtime chart erun-devops <VERSION> not found in registry.example/test (the shared platform chart)
deploy: runtime chart erun-devops <VERSION> not found in ghcr.io/sophium (the shared platform chart in erun's own registry)
deploy: no runtime chart candidate published <VERSION>; falling back to erun-devops in registry.example/test
deploy: no local runtime chart; using published chart oci://registry.example/test/charts/erun-devops version <VERSION>
deploy: defaulting runtime image to the tenant's team-devops image registry.example/test/team-devops:<VERSION> (imageOverrides.erun-devops)
deploy: resolved 1 spec(s)
deploy: worktree volume team-devops-worktree already exists; <HOME>/git/team stays on it
deploy: watching pods in team-dev on context test-context for early container failure (release team-devops)
dedup: claim (release=test-context-team-dev-team-devops, hash=<HASH>, pid=<PID>)
==> Deploying team/dev <VERSION>
    namespace team-dev on context test-context
    waiting for helm rollout (timeout 5m0s)...
==> Deploy of team-devops failed after <ELAPSED>
step timing (ordered by duration):
  deploy (failed) [<ELAPSED>] — runtime chart oci://registry.example/test/charts/erun-devops version <VERSION> could not be pulled from registry.example/test: no chart is published at <VERSION> at any coordinate the deploy probed — registry.example/test/charts/team-devops (the tenant's own umbrella), registry.example/test/charts/erun-devops (the shared platform chart), ghcr.io/sophium/charts/erun-devops (the shared platform chart in erun's own registry). The erun-devops platform chart is published only beside the runtime image erun releases, so a registry holding just this project's own images has it at no version: point the environment at the registry that does, with `erun init <tenant> <env> --runtime-registry <registry>`, which persists it as the env's runtimeregistry and redeploys. If this project publishes its own team-devops umbrella instead, publish it at this version from the project that owns that chart — `erun push --version <VERSION>` (or `erun release`) — then deploy. If the environment rides a chart on another line entirely, state it outright: `runtimechart` in the env config (the desktop's Runtime tab, "Runtime chart") or `--runtime-chart <ref>` for one deploy, and the version keeps naming the image.
Error: failed to perform "FetchReference" on source: registry.example/test/charts/erun-devops: not found
    team/dev <VERSION> (failed) [<ELAPSED>] — runtime chart oci://registry.example/test/charts/erun-devops version <VERSION> could not be pulled from registry.example/test: no chart is published at <VERSION> at any coordinate the deploy probed — registry.example/test/charts/team-devops (the tenant's own umbrella), registry.example/test/charts/erun-devops (the shared platform chart), ghcr.io/sophium/charts/erun-devops (the shared platform chart in erun's own registry). The erun-devops platform chart is published only beside the runtime image erun releases, so a registry holding just this project's own images has it at no version: point the environment at the registry that does, with `erun init <tenant> <env> --runtime-registry <registry>`, which persists it as the env's runtimeregistry and redeploys. If this project publishes its own team-devops umbrella instead, publish it at this version from the project that owns that chart — `erun push --version <VERSION>` (or `erun release`) — then deploy. If the environment rides a chart on another line entirely, state it outright: `runtimechart` in the env config (the desktop's Runtime tab, "Runtime chart") or `--runtime-chart <ref>` for one deploy, and the version keeps naming the image.
Error: failed to perform "FetchReference" on source: registry.example/test/charts/erun-devops: not found
timing record written to <TMP>
==> Deploy failed team/dev: runtime chart oci://registry.example/test/charts/erun-devops version <VERSION> could not be pulled from registry.example/test: no chart is published at <VERSION> at any coordinate the deploy probed — registry.example/test/charts/team-devops (the tenant's own umbrella), registry.example/test/charts/erun-devops (the shared platform chart), ghcr.io/sophium/charts/erun-devops (the shared platform chart in erun's own registry). The erun-devops platform chart is published only beside the runtime image erun releases, so a registry holding just this project's own images has it at no version: point the environment at the registry that does, with `erun init <tenant> <env> --runtime-registry <registry>`, which persists it as the env's runtimeregistry and redeploys. If this project publishes its own team-devops umbrella instead, publish it at this version from the project that owns that chart — `erun push --version <VERSION>` (or `erun release`) — then deploy. If the environment rides a chart on another line entirely, state it outright: `runtimechart` in the env config (the desktop's Runtime tab, "Runtime chart") or `--runtime-chart <ref>` for one deploy, and the version keeps naming the image.
Error: failed to perform "FetchReference" on source: registry.example/test/charts/erun-devops: not found
runtime chart oci://registry.example/test/charts/erun-devops version <VERSION> could not be pulled from registry.example/test: no chart is published at <VERSION> at any coordinate the deploy probed — registry.example/test/charts/team-devops (the tenant's own umbrella), registry.example/test/charts/erun-devops (the shared platform chart), ghcr.io/sophium/charts/erun-devops (the shared platform chart in erun's own registry). The erun-devops platform chart is published only beside the runtime image erun releases, so a registry holding just this project's own images has it at no version: point the environment at the registry that does, with `erun init <tenant> <env> --runtime-registry <registry>`, which persists it as the env's runtimeregistry and redeploys. If this project publishes its own team-devops umbrella instead, publish it at this version from the project that owns that chart — `erun push --version <VERSION>` (or `erun release`) — then deploy. If the environment rides a chart on another line entirely, state it outright: `runtimechart` in the env config (the desktop's Runtime tab, "Runtime chart") or `--runtime-chart <ref>` for one deploy, and the version keeps naming the image.
Error: failed to perform "FetchReference" on source: registry.example/test/charts/erun-devops: not found

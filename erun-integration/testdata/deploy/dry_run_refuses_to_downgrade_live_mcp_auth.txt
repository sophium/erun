audit: erun deploy --dry-run --version <VERSION> team dev
trace-log: would append the full trace to <TMP>
deploy: tenant=team environment=dev version-override=<VERSION> components=[] force=false current=false
deploy: component selection source default (runtime only); deploying the runtime chart alone
deploy: version <VERSION> pinned; installing the published image by reference (deploy never builds)
deploy: mcp auth: release team-devops has it enabled but this deploy resolved none; refusing to downgrade
deploy: spec resolution failed: deploy team/dev: MCP auth is enabled on the live team-devops release, but this deploy resolved none — it would leave the environment's MCP edge unauthenticated. Re-supply the key with --mcp-auth-public-key <path>, or pass --no-mcp-auth to turn authentication off on purpose
==> Deploy failed team/dev: deploy team/dev: MCP auth is enabled on the live team-devops release, but this deploy resolved none — it would leave the environment's MCP edge unauthenticated. Re-supply the key with --mcp-auth-public-key <path>, or pass --no-mcp-auth to turn authentication off on purpose
deploy team/dev: MCP auth is enabled on the live team-devops release, but this deploy resolved none — it would leave the environment's MCP edge unauthenticated. Re-supply the key with --mcp-auth-public-key <path>, or pass --no-mcp-auth to turn authentication off on purpose

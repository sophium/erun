{
  "status": "would-restart",
  "pid": <PID>,
  "controlPort": 26150,
  "preview": [
    {
      "orchestratorId": "erun-ideas",
      "conversationId": "<UUID>",
      "notice": "Reopened erun-ideas on the conversation derived from its id (<UUID>), not the one its last session was working in (<UUID>). That conversation is still there; manage the orchestrator to attach it."
    }
  ]
}
audit: erun app restart --dry-run --output json
app restart: orchestrator=""
app restart: resolving the running desktop app from <TMP>
app restart: status=would-restart pid=<PID> controlPort=26150

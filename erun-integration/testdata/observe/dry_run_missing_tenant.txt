audit: erun observe --dry-run
default tenant is not configured, and one could not be inferred from the working directory either — pass a tenant explicitly with `erun observe --tenant <name>`, or run `erun init --tenant <name> --set-default-tenant` to set a default

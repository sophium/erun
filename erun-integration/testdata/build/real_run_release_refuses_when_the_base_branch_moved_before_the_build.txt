release: resolving project root
release: project root = <TMP>
release: resolving release module root
release: release root = <TMP>
release: loading release config from project
release: main branch = main, develop branch = develop
release: resolving git branch and commit
release: branch = main, commit = <SHORTSHA>
release: resolving base version from VERSION file
release: base version = <VERSION> (from <TMP>
release: classified mode = stable
release: develop branch "develop" exists = false
audit: erun build --release
==> Building
release: branch=main mode=stable version=<VERSION>
next version: <VERSION>
docker image: ghcr.io/sophium/api:<VERSION>
release: worktree clean = true
stage: sync-remote
stage: release
stage: post-release-version-bump
stage: push
==> Build failed after <ELAPSED>
step timing (ordered by duration):
  build (failed) [<ELAPSED>] — origin/main has moved since this release rebased onto it: it carries 3 commit(s) this release is not built on.
The tag <VERSION> and its git refs are already public; only its images and charts are not.
Absorb the move and re-run:
  git -C <TMP> pull --rebase origin main
  erun build --release --force
(--force recreates the local <VERSION> tag this run already made, which the rebase leaves behind.)
    post-release-version-bump [<ELAPSED>]
    push [<ELAPSED>]
    release [<ELAPSED>]
    sync-remote [<ELAPSED>]
timing record written to <TMP>
origin/main has moved since this release rebased onto it: it carries 3 commit(s) this release is not built on.
The tag <VERSION> and its git refs are already public; only its images and charts are not.
Absorb the move and re-run:
  git -C <TMP> pull --rebase origin main
  erun build --release --force
(--force recreates the local <VERSION> tag this run already made, which the rebase leaves behind.)

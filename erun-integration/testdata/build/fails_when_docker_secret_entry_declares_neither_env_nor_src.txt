audit: erun build --dry-run --environment local
cd <TMP> && git check-ignore -q -- .erun/config.yaml
docker.secrets entry "ghcr": declare env (the environment variable holding the credential) or src (the file holding it)

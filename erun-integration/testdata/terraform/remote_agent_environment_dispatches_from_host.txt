audit: erun terraform init --dry-run team dev
terraform: team/dev is a remote-agent environment — dispatching init non-interactively into its own runtime pod instead of running on this machine
kubectl --context test-context --namespace team-dev exec deployment/team-devops -- /bin/sh -lc '<remote-script>'
terraform-dispatch:
  erun terraform init --tenant 'team' --environment 'dev'

audit: erun terraform init --dry-run team dev
terraform team/dev: dev is a remote-agent environment — its terraform state lives on its own runtime pod's home PVC, not on this machine. Run `erun open team dev` for a shell inside the environment, then run `erun terraform ...` from there

CPU: 10.0% of a 1.00-core quota (sampled over 1.0s)
Memory: 394.4MiB / 2.0GiB (19.3%), peak 979.7MiB, OOM kills 0
Note: CPU/Memory above are the runtime container's alone -- an image build runs in the erun-dind sidecar, so this container reads near zero while a build saturates it; `Build CPU` below says why the build cgroup could not be read, so this report has no build figure.
Build CPU: unavailable (the erun-dind sidecar has no build cgroup -- no build has run there under the cap, or its image predates it)
Disk <HOME> 84.9GiB / 189.1GiB (44.9%)
audit: erun usage

# Production overlay. Like erun-zitadel's, the values that matter (the
# external domain, the cert-manager issuer) come from the platform
# environment's config at deploy time, not from this file — a chart default
# is never one instance's own hostname or issuer name.
console:
  ingressClass: traefik

{{- $tenant := default "erun" .Values.tenant -}}
{{- $postgres := default dict (default dict .Values.api).postgres -}}
{{- $postgresDatabase := default "erun" $postgres.database -}}
{{- $postgresUser := default "erun" $postgres.user -}}
{{- $postgresPort := default 5432 $postgres.port -}}
{{- $postgresPasswordSecretName := default (printf "%s-backend-postgres" $tenant) $postgres.passwordSecretName -}}
{{- $postgresPasswordSecretKey := default "password" $postgres.passwordSecretKey -}}
{{- $postgresStorage := default "10Gi" $postgres.storage -}}
{{- $postgresPassword := default "" $postgres.password -}}
{{- $existingPostgresSecret := lookup "v1" "Secret" .Release.Namespace $postgresPasswordSecretName -}}
{{- if and (eq $postgresPassword "") $existingPostgresSecret (hasKey $existingPostgresSecret.data $postgresPasswordSecretKey) -}}
{{- $postgresPassword = (index $existingPostgresSecret.data $postgresPasswordSecretKey | b64dec) -}}
{{- end -}}
{{- if eq $postgresPassword "" -}}
{{- $postgresPassword = randAlphaNum 32 -}}
{{- end -}}
{{- $imageOverrides := default dict .Values.imageOverrides -}}
{{- $containerRegistry := default "ghcr.io/sophium" .Values.containerRegistry -}}
{{- $postgresImage := default (printf "%s/erun-backend-postgres:18.3" $containerRegistry) (index $imageOverrides "erun-backend-postgres") -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $postgresPasswordSecretName }}
  labels:
    app: {{ $tenant }}-postgres
type: Opaque
stringData:
  {{ $postgresPasswordSecretKey }}: {{ $postgresPassword | quote }}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ $tenant }}-postgres-data
  labels:
    app: {{ $tenant }}-postgres
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ $postgresStorage | quote }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ $tenant }}-postgres
  labels:
    app: {{ $tenant }}-postgres
spec:
  type: ClusterIP
  selector:
    app: {{ $tenant }}-postgres
  ports:
    - name: postgres
      port: {{ $postgresPort }}
      targetPort: postgres
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ $tenant }}-postgres
  labels:
    app: {{ $tenant }}-postgres
spec:
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: {{ $tenant }}-postgres
  replicas: 1
  template:
    metadata:
      labels:
        app: {{ $tenant }}-postgres
    spec:
      # No reset logic lives in this pod template, deliberately: this spec is
      # what a crash, eviction, node reboot, or `rollout restart` re-applies,
      # so anything destructive here would re-run on every one of those, not
      # just on a deploy. The reset (reset-job.yaml) is a Helm hook Job that
      # runs once per deploy and never touches the running pod.
      securityContext:
        fsGroup: 999
      containers:
        - image: {{ $postgresImage }}
          name: erun-postgres
          imagePullPolicy: IfNotPresent
          env:
            - name: POSTGRES_DB
              value: {{ $postgresDatabase | quote }}
            - name: POSTGRES_USER
              value: {{ $postgresUser | quote }}
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ $postgresPasswordSecretName }}
                  key: {{ $postgresPasswordSecretKey }}
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          ports:
            - containerPort: {{ $postgresPort }}
              name: postgres
          startupProbe:
            exec:
              command:
                - sh
                - -lc
                - pg_isready -U "$POSTGRES_USER" -d "$POSTGRES_DB" -h 127.0.0.1 -p {{ $postgresPort }}
            periodSeconds: 2
            timeoutSeconds: 2
            failureThreshold: 60
          readinessProbe:
            exec:
              command:
                - sh
                - -lc
                - pg_isready -U "$POSTGRES_USER" -d "$POSTGRES_DB" -h 127.0.0.1 -p {{ $postgresPort }}
            periodSeconds: 5
            timeoutSeconds: 2
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: {{ $tenant }}-postgres-data
      restartPolicy: Always
